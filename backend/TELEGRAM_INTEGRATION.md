# Telegram Integration Guide

## 1. Создание Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Выберите имя для бота (например, "Rose Tic Tac Toe")
4. Выберите username (должен заканчиваться на `bot`, например `rose_tic_tac_toe_bot`)
5. Сохраните полученный **Token** - он будет нужен для настройки

## 2. Настройка Web App

### Локальная разработка:
Для локальной разработки используйте туннель ngrok:

1. Установите ngrok: https://ngrok.com/download
2. Запустите туннель:
   ```bash
   ngrok http 5173
   ```
3. Скопируйте HTTPS URL (например, `https://abcd1234.ngrok.io`)
4. Используйте этот URL в конфигурации бота

### Для продакшена:
1. Задеплойте фронтенд на любой платформе (Vercel, Netlify, etc.)
2. Используйте реальный домен

## 3. Конфигурация

1. Скопируйте `.env.example` в `.env`:
   ```bash
   cp .env.example .env
   ```

2. Отредактируйте `.env`:
   ```env
   TELEGRAM_BOT_TOKEN=ваш_токен_бота_здесь
   WEB_APP_URL=ваш_url_приложения
   ```

## 4. Запуск всего приложения

### Вариант 1: Одновременный запуск (рекомендуется)
```bash
python start_all.py
```

### Вариант 2: Раздельный запуск
```bash
# Terminal 1: Запустить бэкенд
python start_server.py

# Terminal 2: Запустить бота
python telegram_bot.py
```

## 5. Настройка Web App в BotFather

1. Вернитесь к @BotFather
2. Отправьте команду `/mybots`
3. Выберите своего бота
4. Нажмите "Bot Settings" → "Menu Button"
5. Введите текст кнопки (например, "Play Game")
6. Введите URL вашего Web App (ваш HTTPS URL)

## 6. Тестирование

1. Откройте своего бота в Telegram
2. Отправьте команду `/start`
3. Нажмите кнопку "🎮 Play Tic Tac Toe"
4. Приложение должно открыться внутри Telegram

## 7. Получение данных пользователей

Ваше приложение уже использует `useTelegram` hook, который автоматически получает:
- `user.id` - Telegram ID пользователя
- `user.first_name` - Имя
- `user.last_name` - Фамилия (если есть)
- `user.username` - Username (если есть)
- `initData` - Подписанные данные для верификации

Эти данные передаются в бэкенд при каждом запросе.

## 8. Деплой для продакшена

### Backend (на сервере):
```bash
# Установка зависимостей
pip install -r requirements_simple.txt

# Запуск с gunicorn (для продакшена)
pip install gunicorn
gunicorn -w 4 -k uvicorn.workers.UvicornWorker simple_backend:app -b 0.0.0.0:8000
```

### Frontend (на Vercel/Netlify):
1. Перед деплоем сгенерируйте конфигурацию для продакшена:
   ```bash
   python generate_config.py https://your-backend-url.com
   ```
2. Задеплойте React приложение
3. Обновите `WEB_APP_URL` в `.env` на реальный URL
4. Перезапустите бота

### Настройка для Netlify:
1. В панели управления Netlify, в разделе "Build & deploy" -> "Environment" добавьте переменные:
   - `NODE_VERSION` (установите нужную версию Node.js)
2. После деплоя, убедитесь что конфигурация правильно сгенерирована
3. Обновите `WEB_APP_URL` в `.env` для Telegram бота

### Настройка для Vercel:
1. В настройках проекта Vercel, в разделе Environment Variables добавьте:
   - `NEXT_PUBLIC_BACKEND_URL` (если используете Next.js)
2. Сгенерируйте конфигурацию командой выше перед деплоем

### Настройка для GitHub Pages:
1. Запустите `python generate_config.py https://your-username.github.io/repo-name`
2. Задеплойте с помощью GitHub Actions или вручную

ВАЖНО: Файл `public/config.json` используется для динамической настройки URL бэкенда на стороне клиента.

## 9. Полезные ссылки

- [Telegram Web Apps Documentation](https://core.telegram.org/bots/webapps)
- [BotFather Commands](https://core.telegram.org/bots#botfather)
- [python-telegram-bot Documentation](https://docs.python-telegram-bot.org/)